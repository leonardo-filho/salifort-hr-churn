{
  "name": "capstone-project",
  "version": "1.0.0",
  "private": true,
  "description": "Final project for the Google Advanced Data Analytics Professional Certificate.",
  "scripts": {
    "start": "npm run build:frontend && npm run start:backend",
    "start:backend": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "start:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "deploy:frontend": "cd frontend && npm run deploy"
  }
}
